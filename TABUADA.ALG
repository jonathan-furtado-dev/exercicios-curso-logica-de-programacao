Algoritmo "tabuada"

Var

  i, x, igual : inteiro
Inicio

   escreval("Deseja a tabuada de qual número?")
   leia(x)
   
   para i de 1 ate 10050 faca
      igual <- x * i
      escreval(x, " x ", i, " = ", igual)
   fimpara

Fimalgoritmo